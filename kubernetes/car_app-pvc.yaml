apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-pvc
  namespace: carshop
spec:
  storageClassName: hostpath    # Changed from local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
